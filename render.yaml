services:
  - type: web
    name: hms-server
    runtime: node
    buildCommand: npm install
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
